const minNum = /^\d,}$/;
const numAndString = /^[0-9a-zA-Z]*[0-9][0-9a-zA-Z]*$/;
const alphabets = /^[A-Za-z]+$/;
const required = /^$|\s+/;

export const Regex = {
  minNum,
  numAndString,
  alphabets,
  required,
};
