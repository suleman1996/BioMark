import { StyleSheet } from 'react-native';

export const makeStyles = () =>
  StyleSheet.create({
    highlightsView: {
      height: 85,
      width: 187,

      borderRadius: 5,
      //   padding: 5,
      overflow: 'hidden',
    },
  });
export default makeStyles;
