import { StyleSheet } from 'react-native';

export const makeStyles = () =>
  StyleSheet.create({
    container: {
      marginTop: 20,
      alignItems: 'center',
    },
    content: {
      fontSize: 12,
    },
  });

export default makeStyles;
